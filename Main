import sys as s
import math as m
import keyboard

turn_number = 0
move_log = []

while turn_number == 0:
    try:
        if keyboard_is_pressed('g'):
            turn_number += 1
            break
        else:
            pass
    except:
        break
